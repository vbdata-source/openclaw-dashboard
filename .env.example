# ============================================================
# OpenClaw Dashboard — Environment Variables
# ============================================================
# In Coolify: Diese Werte als Environment Variables setzen.
# Lokal: Kopiere als .env und passe an.
# ============================================================

# ── OpenClaw Gateway Verbindung ───────────────────────────
# Container-Name oder Hostname des OpenClaw-Containers
OPENCLAW_HOST=openclaw

# Gateway Port (Standard: 18789)
OPENCLAW_PORT=18789

# Shared Secret — muss in beiden Containern gleich sein
# Generiere mit: openssl rand -hex 32
OPENCLAW_GATEWAY_TOKEN=

# ── Dashboard ─────────────────────────────────────────────
# Login-Passwort für das Dashboard
# Generiere mit: openssl rand -hex 16
DASHBOARD_SECRET=

# Session-Timeout in Minuten (Standard: 480 = 8h)
SESSION_TIMEOUT=480

# Externer Port (Standard: 3200)
DASHBOARD_EXTERNAL_PORT=3200

# ── Docker Netzwerk ───────────────────────────────────────
# Name des Netzwerks in dem OpenClaw läuft
# Finde mit: docker network ls | grep -i openclaw
# Oder in Coolify: Service → Container → Netzwerk-Tab
OPENCLAW_NETWORK_NAME=coolify_default
